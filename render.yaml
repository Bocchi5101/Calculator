services:
  - type: web
    name: orbit-calculator
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webapp:application --bind 0.0.0.0:$PORT --workers 2 --threads 4
    healthCheckPath: /health
